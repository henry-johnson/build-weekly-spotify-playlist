{
  "_comment": "This file is not used by the script. User credentials are loaded from environment variables (SPOTIFY_USER_REFRESH_TOKEN_* secrets). See config/MULTI_USER_SETUP.md for setup instructions."
}
